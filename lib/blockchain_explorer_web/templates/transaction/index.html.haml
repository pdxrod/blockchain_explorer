%span.instructions
  = "Transactions which refer to address #{ @address }"
%br
%span{ id: "please_wait_message", name: "please_wait_message", style: "visibility: hidden; color: #FF7F50" }
  = "Please wait..."
%br

#transactions

  - for transaction <- @transactions do
    %p.transaction_block
      = transaction |> mark_up_transaction |> raw
      %b Outputs:
      %br
      = transaction.outputs |> mark_up_outputs |> raw
      %b Inputs:
      %br
      = transaction.inputs |> mark_up_inputs |> raw

#more_transactions
  = "transactions should appear here"
